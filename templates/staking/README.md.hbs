# {{project_name}}

A staking contract with proportional reward distribution using the O(1)
`acc_reward_per_share` accumulator pattern, generated by
[soroban-forge](https://github.com/soroban-forge-labs/soroban-forge) from the
`staking` template.

## Features

- `deposit` / `withdraw` — manage your stake
- `distribute` — admin injects reward tokens, updating the global accumulator
- `claim` — withdraw your proportional share of rewards (O(1) per user)
- `get_pending_reward` — preview rewards before claiming

## Develop

```sh
cargo test
stellar contract build
```

## Deploy to testnet

The constructor takes `admin` (Address) and `token` (Address of the reward
token contract):

```sh
stellar contract deploy \
  --wasm target/wasm32v1-none/release/{{crate_name}}.wasm \
  --source-account <your-identity> \
  --network testnet \
  -- --admin <ADMIN_ADDRESS> --token <REWARD_TOKEN_ADDRESS>
```

## Security

This template is a starting point, not audited code. See [security-considerations.md](https://github.com/soroban-forge-labs/soroban-forge/blob/main/docs/security-considerations.md) for what it does and does not protect against.
