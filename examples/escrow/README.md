# Escrow Example

Time-locked escrow between two parties.

```sh
forge init my-escrow --template escrow
```

Features:
- Depositor locks funds with a deadline
- Beneficiary can claim before deadline
- Depositor can reclaim after deadline
