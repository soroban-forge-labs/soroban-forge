# Versioning

We follow semantic versioning (SemVer):

- **MAJOR** – breaking API changes
- **MINOR** – new features, backward compatible
- **PATCH** – bug fixes

All releases are tagged as `vX.Y.Z`.
