# Fee Model

Soroban transactions pay:

1. **Base fee** – flat per-transaction
2. **Resource fee** – proportional to CPU, memory, and storage used
3. **Inclusion fee** – bid to prioritise in congestion

Estimate fees with `forge estimate`.
